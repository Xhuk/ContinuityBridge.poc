# ContinuityBridge - Local Development Environment

# === Application Domain Configuration ===
# Your local development domain (or use networkvoid.xyz for testing)
APP_URL=http://localhost:5000
APP_DOMAIN=localhost
EXPORT_CONTACT_EMAIL=dev@localhost

# For production testing with networkvoid.xyz:
# APP_URL=https://networkvoid.xyz
# APP_DOMAIN=networkvoid.xyz
# EXPORT_CONTACT_EMAIL=support@networkvoid.xyz

# === Resend Configuration (Superadmin Emails) ===
# Get free API key from: https://resend.com/api-keys (3,000 emails/month free)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@localhost
# For production: RESEND_FROM_EMAIL=noreply@networkvoid.xyz
# Note: Verify your domain in Resend dashboard first!

# SUPERADMIN Configuration
SUPERADMIN_API_KEY=dev-superadmin-key-change-in-production
# Generate production key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# License Validation Server (use networkvoid.xyz in production)
LICENSE_VALIDATION_URL=http://localhost:5000/api/license/validate
# For production: LICENSE_VALIDATION_URL=https://networkvoid.xyz/api/license/validate

# Organization (for demo/testing)
ORGANIZATION_ID=demo-org
RUNTIME_VERSION=1.0.0

# Node Environment
NODE_ENV=development

# === Authentication Configuration ===
# Auth Guard Toggle: For Founders local development only
# Founders instance: Can toggle for easier local dev
# Customer exports: This setting becomes configurable in their .env
# Set to true to test auth flows locally
AUTH_GUARD_ENABLED=false

# Database (SQLite by default)
# For production PostgreSQL:
# DATABASE_URL=postgresql://user:pass@host:5432/continuitybridge

# SMTP Settings (Required for magic link authentication)
# For Gmail: Enable "App Passwords" in Google Account settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@localhost
# For production: SMTP_FROM=noreply@networkvoid.xyz

# === Customer SMTP Configuration ===
# Customers configure their own SMTP via UI at /settings/email
# This is for flow notifications (errors, validations, acknowledgments)
# Superadmin emails (magic links, invitations) use Resend instead

# Secret Encryption Key
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Optional: AI Mapping Generator
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
