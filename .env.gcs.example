# ==============================================================================
# Google Cloud Storage Configuration
# ==============================================================================
# For deployment package storage (cluster configs, exports, etc.)

# GCP Project ID
GCP_PROJECT_ID=your-gcp-project-id

# GCS Bucket Name
GCS_DEPLOYMENT_BUCKET=continuitybridge-deployments

# Service Account Key (JSON format - base64 encoded or direct JSON)
# Option 1: Use GOOGLE_APPLICATION_CREDENTIALS environment variable (recommended)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2: Inline service account key (for Render/Docker deployments)
# GCP_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"..."}

# Deployment Link Settings
DEPLOYMENT_LINK_EXPIRY_DAYS=7              # Signed URLs expire after X days
DEPLOYMENT_AUTO_DELETE_DAYS=90             # Auto-delete old deployments after X days

# Enable/Disable GCS Storage
DEPLOYMENT_STORAGE_ENABLED=true

# ==============================================================================
# How to Setup GCS:
# ==============================================================================
#
# 1. Create GCP Project:
#    - Go to https://console.cloud.google.com
#    - Create new project or use existing
#
# 2. Create GCS Bucket:
#    - Go to Cloud Storage > Buckets
#    - Create bucket: continuitybridge-deployments
#    - Location: us-central1 (or your region)
#    - Storage class: Standard
#    - Access control: Uniform
#
# 3. Create Service Account:
#    - Go to IAM & Admin > Service Accounts
#    - Create service account: continuitybridge-storage
#    - Grant role: Storage Object Admin
#    - Create JSON key and download
#
# 4. Set Environment Variable:
#    Option A (Local/Docker):
#      export GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
#    
#    Option B (Render):
#      - Upload key.json to Render disk
#      - Set GOOGLE_APPLICATION_CREDENTIALS=/opt/render/project/src/gcp-key.json
#    
#    Option C (Inline):
#      - Copy entire JSON content
#      - Set GCP_SERVICE_ACCOUNT_KEY='{"type":"service_account",...}'
#
# 5. Test Connection:
#    npm run test:gcs
#
# ==============================================================================
# Cost Estimate (with GCP free credits):
# ==============================================================================
# - Storage: 10 GB = $0.20/month (FREE with credits)
# - Operations: 100K = $0.05/month (FREE with credits)
# - Bandwidth: 100 GB = $12/month (FREE with credits)
# 
# Total: ~$12/month normally, $0 with your GCP credits! 🎉
# ==============================================================================
