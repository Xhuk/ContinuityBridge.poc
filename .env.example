# Queue Backend (inmemory | rabbit | kafka)
QUEUE_BACKEND=inmemory

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Database Type: "sqlite" (default, portable) or "postgres" (production)
DB_TYPE=sqlite

# SQLite Configuration (when DB_TYPE=sqlite)
# SQLITE_PATH=./data/continuity.db

# PostgreSQL Configuration (when DB_TYPE=postgres)
# DATABASE_URL=postgresql://user:password@host:5432/database
# Example for Neon: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/database?sslmode=require
# Example for Supabase: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# Example for AWS RDS: postgresql://user:password@your-rds-instance.region.rds.amazonaws.com:5432/database

# PostgreSQL Connection Pool Settings (Optional - for production tuning)
# DB_POOL_MAX=20          # Maximum connections in pool
# DB_POOL_MIN=2           # Minimum connections in pool
# DB_IDLE_TIMEOUT=30000   # Idle timeout in milliseconds
# DB_CONNECTION_TIMEOUT=10000  # Connection timeout in milliseconds

# AI Mapping Generator (Optional - for dev environment only)
# Uncomment and add your API key to enable AI-powered mapping generation
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# AI Assistant (Gemini - Free tier: 15 req/min, 1500 req/day)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Server Ports
PORT=5000
VITE_PORT=5173

# Deployment Type (for routing behavior)
# "platform" = Founder/Consultant platform (uses /sys/auth/bridge for login)
# "customer" = Customer deployment (uses /admin for login)
VITE_DEPLOYMENT_TYPE=platform

# Security Keys (Required for Production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-key-here
JWT_SECRET=your-jwt-secret-key-here
SUPERADMIN_API_KEY=cb_your_api_key_here

# App URL (Required for Production)
APP_URL=http://localhost:5000
# Or use APP_DOMAIN=continuitybridge.com

# Email (Optional - for magic links)
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=noreply@continuitybridge.com

# OAuth (Optional - for Google login)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
